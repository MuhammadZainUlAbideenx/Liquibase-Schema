<?xml version="1.0" encoding="UTF-8"?>

<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="
        http://www.liquibase.org/xml/ns/dbchangelog
        https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <changeSet id="002-create-datadog-user" author="zain">

        <comment>
            Create a read-only PostgreSQL user for Datadog monitoring.
        </comment>

        <!-- Create the role only if it doesn't already exist -->
        <sql splitStatements="false">
            DO $$
            BEGIN
                IF NOT EXISTS (
                    SELECT 1
                    FROM pg_roles
                    WHERE rolname = 'datadog'
                ) THEN
                    CREATE ROLE datadog
                    LOGIN
                    PASSWORD '${datadog.password}';
                END IF;
            END
            $$;
        </sql>

        <!-- Allow connection to the database -->
        <sql>
            GRANT CONNECT ON DATABASE liquibase_demo TO datadog;
        </sql>

        <!-- Allow access to the public schema -->
        <sql>
            GRANT USAGE ON SCHEMA public TO datadog;
        </sql>

        <!-- Read access to all existing tables -->
        <sql>
            GRANT SELECT ON ALL TABLES IN SCHEMA public TO datadog;
        </sql>

        <!-- Automatically grant SELECT on future tables -->
        <sql>
            ALTER DEFAULT PRIVILEGES IN SCHEMA public
            GRANT SELECT ON TABLES TO datadog;
        </sql>

        <!-- Grant PostgreSQL monitoring role -->
        <sql>
            GRANT pg_monitor TO datadog;
        </sql>

    </changeSet>

</databaseChangeLog>